# External Feedback Mathematical Correction Audit

Scope: the ten reported mathematical, attribution, citation, and numbering defects. Language fitting outside those claims is excluded.

## P1 Corrections

1. The residual kernel was already typed on all of \(\mathcal Z_H\), so it was not literally undefined at \(\varnothing\). The real contract gap was that nonzero no-event transitions and law/point coherence were unspecified. The paper now requires the complete kernel behavior, displays the corrected no-event mass, permits probability flow into and out of \(\varnothing\), extends the structured point space by a tagged no-event symbol, and derives the point decision from the corrected law through \(d_H\).
2. The point residual is now \(r_{t,H}^{\mathrm{obs}}\) and exists only for an event inside the originating horizon. A post-horizon event cannot retroactively label an expired forecast. A separate \(\rho_H\) estimator learns distributional residuals from either marked or no-event outcomes, and every cache entry records its horizon and censoring convention.
3. The unknown-target constrained family and infimum are now explicitly oracle objects \(\mathfrak F_{AP}^{\Gamma,\star}\) and \(\mathcal J_\Gamma^{\mathrm{oracle}}\). Operational selection uses a finite predeclared family \(\mathfrak G_\Gamma\), the certified empirical family \(\widehat{\mathfrak F}_{AP}^{\Gamma}\), and a finite argmin. Candidates without exhaustive coverage or the verified continuity certificate are not operationally certifiable.

## P2 Corrections

1. CFS attribution is limited to self-adjoint operator representation. Clipping, projection, admissibility, and the residual objective are identified as EventFrame constructions; the unsupported phrase "variational admissibility" was removed.
2. The proper score is identified as the primary probabilistic-fidelity metric, while the governing system-design objective is explicitly composite with a proper-score non-inferiority guard.
3. A general reference-measure change now adds the outcome-dependent term \(\log h(Z)\); only pure unit rescaling produces a constant shift.
4. Physical references are scoped to a limiting thought experiment for physical substrates. They do not support simulated- or software-substrate compression.
5. Predictive and causal sparsity are separate. \(s_{\mathrm{eff}}^{\mathrm{pred}}\) uses frozen ablations and simultaneous paired confirmation bounds; \(s_{\mathrm{eff}}^{\mathrm{causal}}\) is available only with identified interventions.

## Editorial Corrections

1. Reference 11 is cited for marked point-process background and Reference 12 for concept drift.
2. The experiment sequence is uniquely numbered from one through eleven.

## Mathematical Invariants Checked

1. At \(r=0\), the residual kernel is the identity law and the output bundle is the coherent baseline bundle.
2. For every \(r\), the corrected no-event mass includes transitions from both the existing no-event atom and the marked branch.
3. No \(r_{t,H}^{\mathrm{obs}}\) exists when \(Z_{t+1}=\varnothing\); the law estimator remains defined there.
4. Empty empirical certified families do not relax thresholds or claim oracle feasibility.
5. Confirmation outcomes may classify frozen sparsity candidates but may not refit or regenerate them.

No unresolved P1 or P2 defect from the supplied feedback remains after these source corrections. Rendering and assembled-artifact checks are recorded separately by the build validation.
